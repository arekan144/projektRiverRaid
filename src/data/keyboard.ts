interface keyboard {
    lt: boolean,
    rt: boolean,
    up: boolean,
    bn: boolean,
    space: boolean,
}
export let keyboard: keyboard = {
    lt: false,
    rt: false,
    up: false,
    bn: false,
    space: false
}